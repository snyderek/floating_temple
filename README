Floating Temple
Copyright 2015 Derek S. Snyder

Build Instructions - Short Version
==================================

# Install prerequisites. The following command works on Ubuntu 14.04.1 (Trusty
# Tahr):
sudo apt-get install binutils coreutils g++ gawk gcc git google-perftools \
  libgflags-dev libgoogle-glog-dev libgoogle-perftools-dev libprotobuf-dev \
  make protobuf-compiler scons tar

# Check out the floating_temple source.
mkdir -p ~/projects
cd ~/projects
git clone https://github.com/snyderek/floating_temple.git

# Install the OSSP UUID library.
cd ~/scratch
tar xzf ~/projects/floating_temple/third_party/uuid-1.6.2.tar.gz
cd uuid-1.6.2
./configure
make
make check
sudo make install

# Refresh the shared library cache.
sudo ldconfig

# Add the following line to your ~/.bashrc file:
export PPROF_PATH=/usr/bin/google-pprof

# Build the floating_temple source.
cd ~/projects/floating_temple
scons -j4
scons check
